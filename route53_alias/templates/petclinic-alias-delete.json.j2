{
  "Comment": "Delete alias record for PetClinic",
  "Changes": [
    {
      "Action": "DELETE",
      "ResourceRecordSet": {
        "Name": "{{ petclinic_domain }}",
        "Type": "A",
        "AliasTarget": {
          "HostedZoneId": "{{ alb_info.load_balancers[0].canonical_hosted_zone_id }}",
          "DNSName": "{{ alb_info.load_balancers[0].dns_name }}",
          "EvaluateTargetHealth": false
        }
      }
    }
  ]
}
